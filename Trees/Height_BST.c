//height of tree is number of edges in longest path from root to leaf node
//depth of node is number of edges from root to that node

/*FindHeight(root)
{
    if root==NULL return -1;
    all using recursion
    -find height of left subtree=lh
    -find height of right subtree=rh
    -height of tree is max(lh,rh)+1
}*/
